<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, placeholder: 'enter task to edit', class: 'todo' %>
  <%= f.text_field :details, placeholder: 'details', class: 'details' %>
  <%= f.label :completed %>
  <%= f.check_box :completed %>
  <%= f.submit 'Update task!' %>
<% end %>
